# Left blank on purpose: lib/api.ts derives the API host from the browser
# URL, so the app works at localhost AND at whatever LAN IP DHCP assigns.
# Set this only if the backend runs somewhere other than the same host.
NEXT_PUBLIC_API_BASE_URL=
