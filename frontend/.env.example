# Copy to .env.local (gitignored by the Next.js scaffold's own .gitignore).
# Public vars are inlined into the client bundle — never put a secret here.
# The Ollagraph API key belongs in the BACKEND .env only, never in this file.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
